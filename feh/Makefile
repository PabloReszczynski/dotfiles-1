DEST = $(HOME)

# <parameters>
# </parameters>

# <filepp>
PP_KC = \#>

PP_MODULES =

PP_DEFINES =
# </filepp>

all:
#	filepp -kc '$(PP_KC)' $(PP_MODULES) $(PP_DEFINES) zshrc -o .zshrc

install:
	mkdir -p $(DEST)/.config/feh
	cp keys $(DEST)/.config/feh
	cp themes $(DEST)/.config/feh
